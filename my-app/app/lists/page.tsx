import ListsClient from './ListsClient'

export const metadata = {
  title: 'Lists - Bookmarkd',
  description: 'Discover curated book lists from the community'
}

export default function ListsPage() {
  return <ListsClient />
}
