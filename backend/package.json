{
  "dependencies": {
    "@prisma/client": "^6.11.1",
    "@supabase/supabase-js": "^2.57.4",
    "axios": "^1.10.0",
    "cors": "^2.8.5",
    "dotenv": "^17.1.0",
    "express": "^5.1.0",
    "express-rate-limit": "^8.1.0",
    "ioredis": "^5.7.0",
    "jsonwebtoken": "^9.0.2",
    "jwks-rsa": "^3.2.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10",
    "prisma": "^6.11.1"
  },
  "scripts": {
    "dev": "nodemon server.js",
    "start": "node server.js",
    "seed:demo": "node scripts/seed-books.js --mode=popular --limit=25",
    "seed:small": "node scripts/seed-books.js --mode=popular --limit=100",
    "seed:medium": "node scripts/seed-books.js --mode=genres --limit=1000",
    "seed:large": "node scripts/seed-books.js --mode=genres --limit=5000",
    "seed:custom": "node scripts/seed-books.js --mode=custom",
    "db:monitor": "node scripts/db-monitor.js",
    "db:migrate": "npx prisma migrate deploy",
    "db:generate": "npx prisma generate"
  }
}
